package stream.streamerType;

import stream.Streamer;

public class AutorAudioBook extends Streamer {
    public AutorAudioBook(Integer id,String name) {
        this.setId(id);
        this.setName(name);
    }
}

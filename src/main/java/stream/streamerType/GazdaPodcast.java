package stream.streamerType;

import stream.Streamer;

public class GazdaPodcast extends Streamer {
    public GazdaPodcast(Integer id,String name) {
        this.setId(id);
        this.setName(name);
    }
}

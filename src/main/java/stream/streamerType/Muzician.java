package stream.streamerType;

import stream.Streamer;

public class Muzician extends Streamer {
    public Muzician(Integer id,String name){
        this.setId(id);
        this.setName(name);
    }
}

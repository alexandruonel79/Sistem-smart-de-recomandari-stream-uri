package streams.streamType.streamGenre.audioBooks;

import streams.streamType.AudioBook;

public class PersonalDevelopment extends AudioBook {
    public PersonalDevelopment(Integer id,Long noOfStreams,Integer streamerId,Long length,Long dateAdded,String name) {
        super(id,noOfStreams,streamerId,length,dateAdded,name);
    }
}
